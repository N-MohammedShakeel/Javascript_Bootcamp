/*
------------------------------------------------------
📌 DOM Challenges (Practice)
------------------------------------------------------

1️⃣ Basic DOM Selection & Manipulation
- Select a <p> element with id="text" and change its text to "Hello DOM!"
- Change the background color of the <body> to lightblue.
- Select all <li> items in a <ul> and make their text bold.

------------------------------------------------------

2️⃣ Event Handling
- Add a button with id="btn". When clicked, it should:
    → Change its own text to "Clicked!"
    → Change the background color of the page to yellow.
- Add an input field. When the user types, display the text in a <p> element below.

------------------------------------------------------

3️⃣ Creating & Removing Elements
- Create a new <div> with text "I am new here!" and append it to the body.
- Create a <ul> with 3 new <li> items using JavaScript.
- Remove the second <li> item from the list.

------------------------------------------------------

4️⃣ Intermediate Challenge
- Build a simple counter:
    → Start with a number displayed in <p id="counter">0</p>.
    → Add two buttons: "Increment" and "Decrement".
    → Clicking increment increases the number, decrement decreases it.

------------------------------------------------------

5️⃣ Advanced Challenge
- Make a TODO list:
    → An input box and an "Add" button.
    → When clicking "Add", the text should appear as a new <li> in a list.
    → Each <li> should have a "Delete" button to remove it.
    → Bonus: Save the list in localStorage so it remains after refreshing.

------------------------------------------------------

💡 Hint:
Use these DOM methods frequently:
- document.getElementById()
- document.querySelector() / querySelectorAll()
- element.innerText / textContent / innerHTML
- element.style.property
- document.createElement()
- parent.appendChild(child)
- parent.removeChild(child)
- addEventListener("click", function() { ... })
*/
