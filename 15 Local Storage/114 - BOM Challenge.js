/*
------------------------------------------------------
📌 BOM Challenges (Practice)
------------------------------------------------------

1️⃣ Window & Screen
- Display the current window width and height in the console.
- Show an alert when the window is resized.
- Log the screen width and height (screen.width, screen.height).

------------------------------------------------------

2️⃣ Location Object
- Print the current page URL using window.location.href.
- Create a button "Go to Google" → When clicked, redirect to https://google.com.
- Reload the current page using window.location.reload().

------------------------------------------------------

3️⃣ Navigator Object
- Log the browser name and version using navigator.userAgent.
- Detect if the user is online (navigator.onLine).
- Check if geolocation is available (navigator.geolocation).

------------------------------------------------------

4️⃣ Timing Methods
- Show an alert "Hello after 3 seconds!" using setTimeout().
- Make a digital clock that updates every second using setInterval().
- Create a countdown timer (start from 10 → 0).

------------------------------------------------------

5️⃣ Popups & History
- Use window.open() to open a new window with a given URL.
- Close that window after 5 seconds using window.close().
- Navigate back one page in history using history.back().
- Navigate forward one page in history using history.forward().

------------------------------------------------------

6️⃣ Advanced Challenge
- Build a simple stopwatch:
    → Start button → Starts counting time (using setInterval).
    → Stop button → Stops the stopwatch.
    → Reset button → Resets it to 0.
    → Display the time inside a <p> element.

------------------------------------------------------

💡 Hint: Common BOM methods/properties
- window.innerWidth / innerHeight
- screen.width / screen.height
- location.href / location.reload()
- navigator.userAgent / navigator.onLine
- setTimeout(), setInterval(), clearInterval()
- window.open(), window.close()
- history.back(), history.forward()
*/
